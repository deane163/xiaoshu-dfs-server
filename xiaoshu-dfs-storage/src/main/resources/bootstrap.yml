##  Spring 的配置
spring:
  application:
    name: xiaoshu-dfs-storage
  cloud:
    nacos:
      config:
        server-addr: xiaoshu.aliyun.com:8848
        file-extension: yaml
        prefix: ${spring.application.name}
        group: DEVOPT_GROUP
  ## 环境配置
  profiles:
    active: dev