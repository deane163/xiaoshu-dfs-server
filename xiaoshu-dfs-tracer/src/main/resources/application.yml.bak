## 配置文件已经写入到Nacos中 DataId : xiaoshu-dfs-tracer-dev.yaml  内容如下:
server:
  port: 8000
  servlet:
    context-path: /dfs-tracer

spring:
  application:
    name: xiaoshu-dfs-tracer
  cloud:
    nacos:
      discovery:
        enabled: true
        server-addr: xiaoshu.aliyun.com:8848
  main:
    allow-bean-definition-overriding: true